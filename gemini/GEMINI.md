# ルール

- ユーザーへの応答は日本語で行う
- 実装前に `GEMINI.md` と `/spec` 配下の関連仕様を確認する
- 実装は仕様に基づいて行う
- 仕様が不足している場合は、先に仕様を補う
- 完了した仕様ファイルは `/spec/finished` へ移動する
- `/spec/finished` は、明示的な指示または必要性がある場合のみ参照する

## 注意

- `rm`
- `rm -r`
- `rm -rf`
- `git reset --hard`
- `git push --force`

上記は実行前に確認を取ること
